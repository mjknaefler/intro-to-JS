let productPrice = 10;
let amountInWallet = 30;

//Strict Equal
console.log(productPrice === amountInWallet);

//Strict Non-Equal
console.log(productPrice !== amountInWallet);

//Greate than (>)
console.log(amountInWallet > productPrice);

//Less than (<)
console.log(amountInWallet < productPrice);